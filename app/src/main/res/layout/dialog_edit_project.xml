<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical"
    android:padding="24dp"
    android:background="@drawable/dialog_background"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <!-- Заголовок -->
    <TextView
        android:id="@+id/tv_dialog_title"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/edit_project"
        android:textSize="20sp"
        android:textStyle="bold"
        android:textColor="@color/sc_accent"
        android:paddingBottom="16dp"/>

    <!-- Название -->
    <EditText
        android:id="@+id/et_title"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="@string/project_title"
        android:textColor="@color/sc_text_primary"
        android:textColorHint="@color/sc_text_secondary"
        android:backgroundTint="@color/sc_accent"
        android:inputType="textCapSentences"/>

    <!-- Сумма -->
    <EditText
        android:id="@+id/et_amount"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="@string/amount"
        android:inputType="number"
        android:textColor="@color/sc_text_primary"
        android:textColorHint="@color/sc_text_secondary"
        android:backgroundTint="@color/sc_accent"
        android:layout_marginTop="16dp"/>

    <!-- Статус -->
    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/status"
        android:textColor="@color/sc_text_secondary"
        android:layout_marginTop="24dp"
        android:paddingBottom="8dp"/>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal">

        <TextView
            android:id="@+id/btn_status_in_work"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="@string/in_work"
            android:gravity="center"
            android:padding="12dp"
            android:textColor="@color/sc_text_primary"
            android:background="@drawable/status_button_inactive"/>

        <TextView
            android:id="@+id/btn_status_completed"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="@string/mark_completed"
            android:gravity="center"
            android:padding="12dp"
            android:textColor="@color/sc_text_primary"
            android:background="@drawable/status_button_inactive"
            android:layout_marginStart="12dp"/>

    </LinearLayout>

    <!-- Кнопки -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="end"
        android:layout_marginTop="24dp">

        <TextView
            android:id="@+id/btn_cancel"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="CANCEL"
            android:textColor="@color/sc_text_secondary"
            android:padding="12dp"/>

        <TextView
            android:id="@+id/btn_save"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="SAVE"
            android:textColor="@color/sc_accent"
            android:textStyle="bold"
            android:padding="12dp"
            android:layout_marginStart="16dp"/>

    </LinearLayout>

</LinearLayout>